import styled from "styled-components";

export const Table = styled.table`
  svg {
    position: relative;
    top: 0.4rem;
  }

  th {
    color: #1d5220;
  }

  tr {
    border: 1px solid #363f5f;
    font-weight: bold;
  }

  td svg {
    position: relative;
    top: -0rem;
    cursor: pointer;
  }
`;
